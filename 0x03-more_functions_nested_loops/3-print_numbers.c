#include "holberton.h"

/**
 * print_numbers - Print one digit numbers
 *
 * Return: Integer
 */
int print_numbers(void)
{
	int i;

	for (i = 0; 0 <= 9; i++)
		_putchar(i + '0');
	_putchar('\n')
	return (0);
}
